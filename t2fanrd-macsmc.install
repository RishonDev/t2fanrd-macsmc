post_install() {
  cat <<'EOF'
To enable macsmc fan control, create:
  /etc/modprobe.d/macsmc_hwmon.conf
with:
  options macsmc_hwmon fan_control=1

Then reboot and enable the service:
  sudo systemctl enable --now t2fanrd.service
EOF
}

post_upgrade() {
  post_install
}
